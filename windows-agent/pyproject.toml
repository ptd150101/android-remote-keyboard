[project]
name = "remote-key-agent"
version = "0.1.0"
description = "Windows keyboard injection agent for RemoteKey"
requires-python = ">=3.11,<3.15"
dependencies = []

[dependency-groups]
dev = [
    "pyinstaller==6.21.0",
]

[tool.uv]
package = false
